#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main()
{
    float x,squ;
    printf("Enter the number");
    scanf("%f",&x);
    squ= sqrt(x);
        printf("The square root=%f",squ);

    return 0;
}
