#include <stdio.h>
#include <stdlib.h>

int main()
{
    int num;
    printf("Enter the number:\n");
    scanf("%d",&num);
    if ((num&1)
    {
    printf("LSB of %d is set (1).", num);


    }
    else
            printf("LSB of %d is set (0).", num);

    return 0;
}
