#include <stdio.h>
#include <stdlib.h>

int main()
{
     int x,y,z;
    printf("Enter the length:");
    scanf("%d",&x);
    printf("Enter the breadth:");
    scanf("%d",&y);
    z=x*y;
    printf("area is:%d", z);

    return 0;
}
